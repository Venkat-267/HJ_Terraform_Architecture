pipeline {
    agent any

    environment {
        AWS_ACCESS_KEY_ID = credentials('aws-credentials-id') // Jenkins credential ID
        AWS_SECRET_ACCESS_KEY = credentials('aws-credentials-id') // Jenkins credential ID
        TF_VAR_SECRET_PEM = credentials('demo-pem-as')
    }

    stages {
        stage('Terraform Init') {
            steps {
                sh 'terraform init'
            }
        }

        stage('Terraform Plan') {
            steps {
                sh 'terraform plan -out=tfplan'
            }
        }

        stage('Terraform Apply') {
            steps {
                sh 'terraform apply -auto-approve tfplan'
            }
        }

        stage('Get EC2 IPs') {
            steps {
                script {
                    // Capture the public IP addresses from Terraform output
                    def tfOutput = sh(script: 'terraform output -json ec2_public_ips', returnStdout: true).trim()
                    env.EC2_PUBLIC_IPS = readJSON(text: tfOutput)
                }
            }
        }

        stage('Knife Bootstrap') {
            steps {
                script {
                    // Iterate over the tuple of IPs and bootstrap each EC2 instance
                    for (int i = 0; i < env.EC2_PUBLIC_IPS.size(); i++) {
                        def publicIp = env.EC2_PUBLIC_IPS[i]
                        sh """
                        cd /node-chef
                        knife bootstrap ${publicIp} \
                        --ssh-user ec2-user \
                        --sudo \
                        --identity-file ${TF_VAR_SECRET_PEM} \
                        --node-name node-${i} \
                        --run-list 'recipe[node-js]'
                        """
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Terraform deployment successful!'
        }
        failure {
            echo 'Terraform deployment failed.'
        }
    }
}
